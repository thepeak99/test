containers:
  - context: .
    tag: {{ .Secrets.Image }}:{{ .Commit }}
    dockerfile: Dockerfile.test
    args:
      ARG1: patata

actions:
  - type: push
    image: {{ .Secrets.Image }}:{{ .Commit }}
    provider: aws
  - type: exec
    image: {{ .Secrets.Image }}:{{ .Commit }}
    commands:
      - ls
      - echo pito
notifiers:
  - type: github
  - type: generic
    url: tato

